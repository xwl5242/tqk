server:
  port: 8083
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xwl5242/tqk-config
  application:
    name: tqk-config
eureka:
  instance:
    prefer-ip-address: true
    hostname: tqk-config
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://tqk-eureka:8761/eureka/
