spring:
  application:
    name: tqk-web
  cloud:
    config:
      discovery:
        enabled: true
        service-id: tqk-config
      profile: 'dev'
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://tqk-eureka:8761/eureka/