spring:
  application:
    name: tqk-web
  cloud:
    config:
      discovery:
        enabled: true
        service-id: tqk-config
      profile: 'pro'
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://172.17.0.2:8761/eureka/